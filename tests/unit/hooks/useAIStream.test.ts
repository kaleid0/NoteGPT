import { test } from 'vitest'

// Test moved to useAIStream.test.tsx (TSX required for JSX parsing).
// This file contains a skipped placeholder to avoid 'no test suite found' during watch.
test.skip('moved to .tsx', () => {})
